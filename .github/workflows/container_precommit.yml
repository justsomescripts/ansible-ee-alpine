name: Check code

on:
  pull_request:
    branches:
      - release
    types:
      - opened

permissions: write-all

jobs:
  call-workflow:
    uses: justsomescripts/common-actions/.github/workflows/container-build.yml@main
    secrets: inherit
    with:
      check_only: true
