name: Build Container Image

on:
  push:
    branches:
      - main
    paths-ignore: 
      - '.github/workflows/**'
      - '.github/release.yml'
      - 'README.md'
  pull_request:
    branches:
      - release
    types:
      - closed

permissions: write-all

jobs:
  call-workflow:
    uses: justsomescripts/common-actions/.github/workflows/container-build.yml@main
    secrets: inherit
    with:
      containerfile: ./Containerfile
